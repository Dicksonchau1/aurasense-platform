'use client';

import { AccountManagerDashboard } from '@/atlas/internal/AccountManagerDashboard';

export const dynamic = 'force-dynamic';

export default function Page() {
  return <AccountManagerDashboard />;
}
