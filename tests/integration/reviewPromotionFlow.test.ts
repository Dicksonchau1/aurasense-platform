describe('End-to-End Review and Promotion Flow', () => {
  it('should trigger review modal, submit decision, promote, and allow replay', async () => {
    // Simulate planner agent output with tier='review'
    // Trigger ReviewModal, submit justification, call recordReview
    // Approve and promote, create scene snapshot, link to promotion
    // Replay and verify scene/trajectory load
  });
});