import React from "react";

const AtlasSurfaceLayout: React.FC<{ children: React.ReactNode }> = ({ children }) => (
  <div className="atlas-surface-layout">
    {children}
  </div>
);

export { AtlasSurfaceLayout };
